#
# Just tell galfast to use the fitsout module for output If you want to
# change the default output filename (which is sky.fits), use galfast's
# --output=filename.fits command line option.
#

module = fitsout
